// Pipeline handlers land here in M1-T4 (EPISODE_SPLIT, EXTRACT_ASSETS, …).
// Importing this module registers them all via registerHandler().
// TEST_ECHO is registered in ../registry.ts itself.
export {};
